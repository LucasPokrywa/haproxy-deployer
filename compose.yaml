services:
  haproxy:
    build:
      context: .
      dockerfile: haproxy/Dockerfile
    env_file:
      - .env
    ports:
      - "${HAPROXY_HTTP_PUBLIC_PORT:-80}:${HAPROXY_HTTP_LISTEN_PORT:-80}"

  benchmark:
    build:
      context: ./benchmark
    env_file:
      - .env
    profiles:
      - benchmark
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      - haproxy